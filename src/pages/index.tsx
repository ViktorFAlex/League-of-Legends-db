import DefaultLayout from '@/components/DefaultLayout';
import HomePage from '@/components/HomePage';

const Home = () => (
  <DefaultLayout>
    <HomePage />
  </DefaultLayout>
);

export default Home;
